import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { ArrowLeft, ArrowRight } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import Logo from '../components/Logo';
export default function Auth({ mode }) { const isRegister=mode==='register'; const {login,register}=useAuth(); const navigate=useNavigate(); const [form,setForm]=useState({name:'',email:'',password:''}); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const submit=async(e)=>{e.preventDefault();setError('');setBusy(true);try{await(isRegister?register(form):login(form));navigate('/app')}catch(err){setError(err.message)}finally{setBusy(false)}}; return <div className="auth-page"><Link to="/" className="back-link"><ArrowLeft size={16}/> Back to home</Link><section className="auth-card"><Logo/><div className="auth-heading"><h1>{isRegister?'Create your workspace':'Welcome back'}</h1><p>{isRegister?'A calmer way to plan, build, and ship.':'Pick up exactly where your team left off.'}</p></div><form onSubmit={submit}>{isRegister&&<label>Full name<input autoFocus required minLength="2" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Ada Lovelace"/></label>}<label>Email address<input autoFocus={!isRegister} required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@company.com"/></label><label>Password<input required type="password" minLength="8" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters"/></label>{error&&<div className="form-error">{error}</div>}<button className="button primary full large" disabled={busy}>{busy?'Please wait…':isRegister?'Create account':'Log in'}<ArrowRight size={17}/></button></form><p className="auth-switch">{isRegister?'Already have an account?':'New to Kira?'} <Link to={isRegister?'/login':'/register'}>{isRegister?'Log in':'Create an account'}</Link></p></section><aside className="auth-quote"><blockquote>“We replaced three status meetings with one Kira workspace.”</blockquote><p><strong>Maya Chen</strong><span>Product lead at Northstar</span></p></aside></div>; }

